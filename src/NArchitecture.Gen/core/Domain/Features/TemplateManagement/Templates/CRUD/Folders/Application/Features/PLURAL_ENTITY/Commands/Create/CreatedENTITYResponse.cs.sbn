using NArchitecture.Core.Application.Responses;{{ for propertyItem in entity.properties }}{{ if (propertyItem.name_space) }}
using {{ propertyItem.name_space }};{{ end }}{{ end }}

namespace {{ project_name }}.Application.Features.{{ entity.name | string.pascalcase | string.plural }}.Commands.Create;

public record Created{{ entity.name | string.pascalcase }}Response(
    {{ entity.id_type }} Id{{ for propertyItem in entity.properties }},
    {{ propertyItem.type }} {{ propertyItem.name | string.pascalcase }}{{ end }}
) : IResponse;