namespace {{ project_name }}.Application.Features.{{ feature_name | string.pascalcase }}.Constants;

public static class {{ feature_name | string.pascalcase }}OperationClaims
{
    private const string _section = "{{ feature_name | string.pascalcase }}";

    public const string Admin = $"{_section}.Admin";

    public const string Read = $"{_section}.Read";
    public const string Write = $"{_section}.Write";
}