@if(entity.BaseType == "User")
{
    // User sınıfı için özel işlem
    public DbSet<{{ entity.name | string.pascalcase }}> {{ entity.name | string.plural }} { get; set; }
}
@else
{
    // Diğer Entity türleri için normal işlem
    public DbSet<{{ entity.name | string.pascalcase }}> {{ entity.name | string.plural }} { get; set; }
}
